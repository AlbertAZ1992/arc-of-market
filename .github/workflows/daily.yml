name: daily-update

on:
  schedule:
    - cron: "17 2 * * 2-6"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: data-update
  cancel-in-progress: false

jobs:
  update:
    uses: ./.github/workflows/update-data.yml
    secrets: inherit
